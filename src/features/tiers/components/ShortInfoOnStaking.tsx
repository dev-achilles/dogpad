import * as React from "react";
import { Alert } from "react-bootstrap";

export const ShortInfoOnStaking = () => (
  <div className="space-y-3 font-light ">
    <p>
      Once staked, you need to <b>register for IDOs</b>, so we can calculate the
      guaranteed allocation. Once registered, we <b>lock your tokens</b>, but
      you still <b>can participate in other IDOs</b>.
    </p>
  </div>
);
